import Splide from '@splidejs/splide';


  new Splide('.splide', {
    type: 'loop',
    // perPage: 1,
    autoWidth: true,
    easing: 'cubic-bezier(0.25, 1, 0.5, 1)',
    padding: 100,
  }).mount();

